# Notification System React Console

## Run

```bash
cd webapp
npm install
npm run dev
```

App runs on `http://localhost:5173` by default.

Backend base URL defaults to `http://localhost:8080` and can be changed in the UI header.

## Build

```bash
npm run build
```
